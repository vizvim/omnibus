// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: omnibus/v1/events.proto

package omnibusv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/vizvim/omnibus/gen/go/omnibus/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// EventServiceName is the fully-qualified name of the EventService service.
	EventServiceName = "omnibus.v1.EventService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// EventServiceStreamEventsProcedure is the fully-qualified name of the EventService's StreamEvents
	// RPC.
	EventServiceStreamEventsProcedure = "/omnibus.v1.EventService/StreamEvents"
)

// EventServiceClient is a client for the omnibus.v1.EventService service.
type EventServiceClient interface {
	// StreamEvents pushes a typed event envelope to the subscribed client as job/download/
	// issue producers publish to the in-process bus. It returns cleanly when the client
	// context is canceled (no leak).
	StreamEvents(context.Context, *connect.Request[v1.StreamEventsRequest]) (*connect.ServerStreamForClient[v1.EventEnvelope], error)
}

// NewEventServiceClient constructs a client for the omnibus.v1.EventService service. By default, it
// uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewEventServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) EventServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	eventServiceMethods := v1.File_omnibus_v1_events_proto.Services().ByName("EventService").Methods()
	return &eventServiceClient{
		streamEvents: connect.NewClient[v1.StreamEventsRequest, v1.EventEnvelope](
			httpClient,
			baseURL+EventServiceStreamEventsProcedure,
			connect.WithSchema(eventServiceMethods.ByName("StreamEvents")),
			connect.WithClientOptions(opts...),
		),
	}
}

// eventServiceClient implements EventServiceClient.
type eventServiceClient struct {
	streamEvents *connect.Client[v1.StreamEventsRequest, v1.EventEnvelope]
}

// StreamEvents calls omnibus.v1.EventService.StreamEvents.
func (c *eventServiceClient) StreamEvents(ctx context.Context, req *connect.Request[v1.StreamEventsRequest]) (*connect.ServerStreamForClient[v1.EventEnvelope], error) {
	return c.streamEvents.CallServerStream(ctx, req)
}

// EventServiceHandler is an implementation of the omnibus.v1.EventService service.
type EventServiceHandler interface {
	// StreamEvents pushes a typed event envelope to the subscribed client as job/download/
	// issue producers publish to the in-process bus. It returns cleanly when the client
	// context is canceled (no leak).
	StreamEvents(context.Context, *connect.Request[v1.StreamEventsRequest], *connect.ServerStream[v1.EventEnvelope]) error
}

// NewEventServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewEventServiceHandler(svc EventServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	eventServiceMethods := v1.File_omnibus_v1_events_proto.Services().ByName("EventService").Methods()
	eventServiceStreamEventsHandler := connect.NewServerStreamHandler(
		EventServiceStreamEventsProcedure,
		svc.StreamEvents,
		connect.WithSchema(eventServiceMethods.ByName("StreamEvents")),
		connect.WithHandlerOptions(opts...),
	)
	return "/omnibus.v1.EventService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case EventServiceStreamEventsProcedure:
			eventServiceStreamEventsHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedEventServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedEventServiceHandler struct{}

func (UnimplementedEventServiceHandler) StreamEvents(context.Context, *connect.Request[v1.StreamEventsRequest], *connect.ServerStream[v1.EventEnvelope]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("omnibus.v1.EventService.StreamEvents is not implemented"))
}
